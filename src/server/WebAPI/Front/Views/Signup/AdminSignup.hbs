<div class="row no-gutters min-h-fullscreen bg-white">
    <div class="col-md-6 col-lg-7 col-xl-8 d-none d-md-block bg-img" style="background-image: url({{AssetURL}}/img/forest.jpg)"
        data-overlay="5">

        <div class="row h-100 pl-50">
            <div class="col-md-10 col-lg-8 align-self-end">
                <img src="{{AssetURL}}/img/logo-lg-user-signup.svg" alt="...">
                <br>
                <br>
                <br>
                <h4 class="text-white">{{l10n "FAST AND SECURE ENTERPRISE MESSAGING"}}</h4>
                <p class="text-white">
                    {{l10n "Spika is a most flexible enterprise messaging app. You can customise it, change it, integrate with any other service. Spika will fits all your business demands for all business phases."}}
                </p>
                <br>
                <br>
            </div>
        </div>

    </div>

    <div class="col-md-6 col-lg-5 col-xl-4 align-self-center">
        <div class="px-80 py-30">
            <h4>{{l10n "Create new organization"}}
            </h4>
            <br> {{> alertMessage }}

            <div class="callout callout-success" role="alert">
                <p>
                    {{l10n "Select your organization name, and please input information to signin as organization admin."}}
                </p>
            </div>

            <form id="mainform" method="post" class="form-type-material">

                {{!-- this line is to prevent google-s autofill  --}}
                <input style="display:none;" type="password" name="password" />

                <div class="form-group">
                    <input type="text" class="form-control" name="organizationId" id="text-organizationId" type="text" placeholder="{{l10n "Enter organization ID..."}}"
                        value="{{organizationId}}" {{{setFocus "organizationId" focusField}}}>
                </div>

                <div class="form-group">
                        <input class="form-control" name="username" id="text-name" type="text" placeholder="{{l10n "Enter user name..."}}" value="{{username}}" {{{setFocus "username" focusField}}}>
                </div>

                <div class="form-group">
                        <input class="form-control" name="password" id="text-password" type="password" placeholder="{{l10n "Enter password"}}" value="{{password}}" {{{setFocus "password" focusField}}}> 
                </div>

                <div class="form-group">
                        <input class="form-control" name="reenterPassword" id="text-reenterPassword" type="password" placeholder="{{l10n "Re-enter password"}}" value="{{reenterPassword}}" {{{setFocus "reenterPassword" focusField}}}> 
                </div>

                <div class="form-group">
                        <input class="form-control" name="email" id="text-email" type="email" placeholder="{{l10n "Enter e-mail (optional)..."}}" value="{{email}}" {{{setFocus "email" focusField}}}>
                </div>

                <div class="form-group">
                    <button class="btn btn-bold btn-block btn-primary" type="submit">{{l10n "SignUp"}}</button>
                </div>

                <p class="text-center text-muted fs-13 mt-20">{{l10n "Allready a member?"}}
                    <a class="text-primary fw-500" href="/">{{l10n "Sign in"}}</a>
                </p>

            </form>

        </div>
    </div>
</div>