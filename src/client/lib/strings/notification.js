import * as config from '../config';
import {EN} from '../const';

export const NotificationNoNotification = {
    EN:"No new notifications."
}

export const NotificationReadAll = {
    EN:"Read all notifications."
}
