import * as config from '../config';
import {EN} from '../const';

export const SearchTitle = {
    EN:"Search Messages"
}

export const FailedToSearchMessage = {
    EN:"Failed to search message. Please try again later."
}